{
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "validate": "pnpm --filter @commercetools/processors run validate && pnpm --filter @commercetools/commerce-agent run validate && pnpm --filter @commercetools/commerce-mcp run validate",
    "build": "pnpm --filter @commercetools/processors run build && pnpm --filter @commercetools/commerce-agent run build && pnpm --filter @commercetools/commerce-mcp run build",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "test": "pnpm run -r test"
  },
  "dependencies": {
    "@changesets/changelog-github": "0.5.1",
    "@changesets/cli": "^2.29.4",
    "@commercetools/commerce-agent": "workspace:*",
    "@commercetools/processors": "workspace:*",
    "@commercetools/platform-sdk": "^9.4.0",
    "@commercetools/checkout-sdk": "^1.0.0",
    "@commercetools/tools-core": "^0.3.0",
    "@commercetools/ts-client": "^5.0.2",
    "@types/pluralize": "^0.0.33"
  },
  "packageManager": "pnpm@9.11.0"
}
